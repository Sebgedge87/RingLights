// ── WiFi Credentials ──────────────────────────────────────
// Copy this file to credentials.h and fill in your network details.
// credentials.h is gitignored — never commit real credentials.
#pragma once
#define WIFI_SSID "your_network_name"
#define WIFI_PASS "your_password"
